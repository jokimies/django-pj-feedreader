============
Installation
============

At the command line::

    $ easy_install django-pj-feedreader

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-pj-feedreader
    $ pip install django-pj-feedreader
